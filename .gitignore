# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*
!.env.local.example

# moodboard (material de diseño local, no debe ir al repo)
/moodboard/

# playwright MCP (logs de auditoría local, no va al repo)
/.playwright-mcp/

# gentle-ai skill registry (cache local de tooling, no va al repo)
/.atl/

# capturas temporales raíz (auditorías visuales con Playwright MCP)
/*.png
/Logo/
/Logo /

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# dev server log
dev-server.log
# Local Pi runtime state
.atl/

# local responsive/performance audit artifacts
/.codegraph/
/.ui-audit/
*.log
